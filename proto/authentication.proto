syntax="proto3";
package Authentication;

service Authenticate {
    rpc AuthenticateUser (AuthRequest) returns (AuthReply);
}

message AuthRequest {
    string login = 1;
    string password = 2;
}

message AuthReply {
    string token = 1;
}